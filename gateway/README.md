# Gateway

DingTalk messaging gateway configuration.

```yaml
platforms:
  dingtalk:
    extra:
      client_id: "your_client_id"
      client_secret: "your_client_secret"
```

See `config.yaml.example` in this directory for a full configuration template.
